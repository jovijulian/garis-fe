import Support from "./support";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Support | GARIS PT. Cisangkan",
};

export default function SupportPage() {
  return <Support />;
}
