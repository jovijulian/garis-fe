import Slug from "./slug";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tutorial | GARIS PT. Cisangkan",
};

export default function SlugPage() {
  return <Slug />;
}
